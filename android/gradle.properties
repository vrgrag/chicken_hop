org.gradle.jvmargs=-Xmx6G -XX:MaxMetaspaceSize=3G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true

# Required on Windows when the project path contains a space, non-ASCII
# char, or any path component that the Kotlin RelocatableFileToPathConverter
# can't reduce relative to the project root.
kotlin.incremental=false

# Added automatically by the Flutter migrator — keep both.
android.builtInKotlin=false
android.newDsl=false
